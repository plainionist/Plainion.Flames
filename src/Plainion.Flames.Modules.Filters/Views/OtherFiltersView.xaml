<UserControl x:Class="Plainion.Flames.Modules.Filters.Views.OtherFiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <CheckBox IsChecked="{Binding InterpolateBrokenStackCalls}" Margin="3">
            <TextBlock TextWrapping="Wrap">
                <Run>Interpolate call with broken stacks</Run>
                <LineBreak/>
                <Run>
                    The calls containing broken stacks will be removed. 
                    The call before the broken one will be enlarged by the duration of the removed call.
                    The calls before and after the broken one will be merged upwards the stack as long as the method is the same.
                </Run>
            </TextBlock>
        </CheckBox>
    </Grid>
</UserControl>
